name: CI/CD Pipeline

on:
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_IDS }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Run Trivy SAST scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: "fs"
          path: .

      - name: Build Docker image
        run: |
          cd ./dummy-app
          docker build -t ${{ secrets.AWS_ECR_URI }}:${{ github.sha }} .

      - name: Log in to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v1
        with:
          region: ${{ secrets.AWS_REGION }}

      - name: Push Docker image to ECR
        run: |
          docker tag ${{ secrets.AWS_ECR_URI }}:${{ github.sha }} ${{ secrets.AWS_ECR_URI }}:latest
          docker push ${{ secrets.AWS_ECR_URI }}:${{ github.sha }}
          docker push ${{ secrets.AWS_ECR_URI }}:latest
      
      - name: Update Argo CD manifest for frontend
        run: |
          echo "Contents of ArgoCD/frontend.yaml before update:"
          cat ArgoCD/frontend.yaml
          sed -i 's|image: .*|image: ${{ secrets.AWS_ECR_URI }}:${{ github.sha }}|' ArgoCD/frontend.yaml
          sed -i 's|image: .*|image: ${{ secrets.AWS_ECR_URI }}:latest|' ArgoCD/frontend.yaml
          echo "Contents of ArgoCD/frontend.yaml after update:"
          cat ArgoCD/frontend.yaml

      - name: Update Argo CD manifest for backend
        run: |
          echo "Contents of ArgoCD/backend.yaml before update:"
          cat ArgoCD/backend.yaml
          sed -i 's|image: .*|image: ${{ secrets.AWS_ECR_URI }}:${{ github.sha }}|' ArgoCD/backend.yaml
          sed -i 's|image: .*|image: ${{ secrets.AWS_ECR_URI }}:latest|' ArgoCD/backend.yaml
          echo "Contents of ArgoCD/backend.yaml after update:"
          cat ArgoCD/backend.yaml

      - name: Commit updated manifest files
        run: |
          git config user.email "tonytoast3006@gmail.com"
          git config user.name "Patkarornj"
          git add ArgoCD/frontend.yaml
          git add ArgoCD/backend.yaml
          git commit -m "Update Argo CD manifests with new image tag"
          git pull --rebase
          git push